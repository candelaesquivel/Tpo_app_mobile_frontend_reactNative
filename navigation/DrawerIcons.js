import {Icon} from '@rneui/base';

export const ModifyDataIcon = ({focused, color, size}) => {
  return <Icon name="badge" />;
};

export const FavoritesIcon = ({focused, color, size}) => {
  return <Icon name="favorite" />;
};

export const HomeIcon = ({focused, color, size}) => {
  return <Icon name="home" />;
};

export const LogoutIcon = ({focused, color, size}) => {
  return <Icon name="power-settings-new" />;
};

export const DeleteIcon = ({focused, color, size}) => {
  return <Icon name="delete" />;
};

export const MyRestaurantsIcon = ({focused, color, size}) => {
  return <Icon name="restaurant" />;
};
